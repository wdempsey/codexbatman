# Rubric: Execution Mode

## Passing Standard

The output passes if it:

- identifies the correct canonical skill
- executes the shared workflow directly
- produces the required artifact
- keeps assumptions concise and visible

## Failure Modes

Fail if the output:

- drifts into tutoring
- skips the artifact
- ignores workflow guardrails
