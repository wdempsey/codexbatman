# Practitioner Overlays

This directory contains overlays for the working data scientist role.

- These overlays wrap canonical skills rather than replacing them.
- The purpose is direct execution with strict artifact discipline.
- Default behavior is to move the workflow forward, produce the required artifact, and avoid unnecessary tutoring or managerial abstraction.

Available overlays:

- `execution-mode`
- `artifact-enforcer`
